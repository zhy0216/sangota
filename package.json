{
  "name": "sangota",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "三国杀戮尖塔 — a Three Kingdoms themed roguelike deckbuilder",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vitest": "^4.1.10"
  }
}
